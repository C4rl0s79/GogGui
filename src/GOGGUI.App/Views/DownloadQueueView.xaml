<Page
    x:Class="GOGGUI.Views.DownloadQueueView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:models="using:GOGGUI.Core.Models">

    <Grid Padding="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Spacing="16" Margin="0,0,0,16">
            <TextBlock Text="Download Queue" FontSize="24" FontWeight="Bold"
                       VerticalAlignment="Center"/>
            <Border x:Name="ActiveCountBadge" Background="#0969da" CornerRadius="10"
                    Padding="8,2" VerticalAlignment="Center">
                <TextBlock x:Name="ActiveCountLabel" Foreground="White" FontSize="12"/>
            </Border>
        </StackPanel>

        <!-- Toolbar -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Spacing="8" Margin="0,0,0,14">
            <Button x:Name="CancelAllButton" Content="⛔ Cancel all"
                    Click="CancelAllButton_Click"/>
            <Button x:Name="ClearFinishedButton" Content="🗑️ Clear finished"
                    Click="ClearFinishedButton_Click"/>
            <TextBlock x:Name="SlotLabel" FontSize="12" Opacity="0.6"
                       VerticalAlignment="Center"/>
        </StackPanel>

        <!-- Queue list -->
        <ListView x:Name="QueueList" Grid.Row="2"
                  SelectionMode="None">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="models:DownloadJob">
                    <Grid Padding="4,10" ColumnSpacing="12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="160"/>
                            <ColumnDefinition Width="90"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <!-- Title -->
                        <TextBlock Grid.Column="0" Grid.Row="0"
                                   Text="{x:Bind Title}" FontSize="13" FontWeight="SemiBold"
                                   TextTrimming="CharacterEllipsis"/>

                        <!-- Progress text -->
                        <TextBlock Grid.Column="0" Grid.Row="1"
                                   Text="{x:Bind ProgressText, Mode=OneWay}"
                                   FontSize="11" Opacity="0.55"
                                   TextTrimming="CharacterEllipsis"
                                   Margin="0,2,0,0"/>

                        <!-- Progress bar -->
                        <ProgressBar Grid.Column="1" Grid.Row="0" Grid.RowSpan="2"
                                     Value="{x:Bind ProgressPercent, Mode=OneWay}"
                                     Maximum="100"
                                     VerticalAlignment="Center"
                                     IsIndeterminate="{x:Bind IsActive, Mode=OneWay}"/>

                        <!-- Status label -->
                        <TextBlock Grid.Column="2" Grid.Row="0" Grid.RowSpan="2"
                                   Text="{x:Bind StatusLabel, Mode=OneWay}"
                                   FontSize="12"
                                   VerticalAlignment="Center"
                                   HorizontalAlignment="Center"/>

                        <!-- Cancel button -->
                        <Button Grid.Column="3" Grid.Row="0" Grid.RowSpan="2"
                                Content="✕"
                                Tag="{x:Bind}"
                                Click="CancelJobButton_Click"
                                Padding="8,4"
                                VerticalAlignment="Center"
                                IsEnabled="{x:Bind IsActive, Mode=OneWay}"
                                ToolTipService.ToolTip="Cancel this download"/>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
