<Page
    x:Class="GOGGUI.Views.LibraryView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:models="using:GOGGUI.Core.Models">

    <Grid Padding="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Toolbar -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Spacing="10" Margin="0,0,0,20">
            <Button x:Name="RefreshButton" Content="Refresh Library" Click="RefreshButton_Click"/>
            <Button x:Name="LoginButton" Content="Login" Click="LoginButton_Click"/>
            <Button x:Name="UpdateMetadataButton" Content="Update Metadata" Click="UpdateMetadataButton_Click"/>
            <ToggleSwitch x:Name="ExtrasToggle" Header="Include Extras"
                          OffContent="No" OnContent="Yes" Margin="20,0,0,0"/>
        </StackPanel>

        <!-- Games Grid -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <ItemsRepeater x:Name="GamesRepeater">
                <ItemsRepeater.Layout>
                    <UniformGridLayout MinItemWidth="200" MinItemHeight="260"
                                       ItemsStretch="Fill" MinColumnSpacing="12" MinRowSpacing="12"/>
                </ItemsRepeater.Layout>
                <ItemsRepeater.ItemTemplate>
                    <DataTemplate x:DataType="models:GameState">
                        <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                                CornerRadius="8" Padding="12">
                            <StackPanel Spacing="8">
                                <TextBlock Text="{x:Bind Title}" FontSize="14" FontWeight="SemiBold"
                                           TextWrapping="Wrap" MaxLines="2"/>
                                <TextBlock Text="{x:Bind Slug}" FontSize="11" Opacity="0.6"/>
                                <ProgressBar Value="0" Maximum="100" Height="4"
                                             Foreground="{ThemeResource AccentAAFillColorDefaultBrush}"/>
                                <TextBlock Text="{x:Bind Status}" FontSize="11"
                                           Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}"/>
                                <Button Content="Download" HorizontalAlignment="Stretch"
                                        Tag="{x:Bind Slug}" Click="DownloadButton_Click"/>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsRepeater.ItemTemplate>
            </ItemsRepeater>
        </ScrollViewer>
    </Grid>
</Page>
